create database comtba;

use  comtba;
create table logIn(
 userName varchar(10),
 userpass varchar(20)
 );
 
 insert into  comtba.login values('a','a');
 
use  comtba;
create table UserData(
 FirstName varchar(20),
 LastName varchar(20),
 UserName varchar(20),
 Password varchar(20),
 Email varchar(30),
 Birthday varchar(20),
 Gender varchar(10)
 );
 
 